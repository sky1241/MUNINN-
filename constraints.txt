# CHUNK C1 (2026-05-08): pin optional dependencies for reproducible builds.
#
# Pre-fix: pyproject.toml used `tiktoken>=0.5`, `anthropic>=0.20` — any
# `pip install -e .[all]` could pick up a 2-year-old version OR a
# brand-new breaking release. Builds were not reproducible across
# environments / CI / fresh machines.
#
# Strategy: pin the currently-tested versions here as upper bounds.
# Use via:  pip install -c constraints.txt -e '.[all]'
# Or in CI: pip install -c constraints.txt -r requirements-dev.txt
#
# Update procedure: bump the pin, re-run `pytest tests/`, commit the
# new pin only if all tests pass. Dependabot (CHUNK C13) will open
# PRs that propose bumps.
#
# Source: docs/CHUNKS_AUDIT2_FIX_LIST_2026-05-08.md §C1

# Token counting (used by L4, L12, scoring) — currently tested with 0.12.0
tiktoken==0.12.0

# Optional LLM compression (L9) — bumped to 0.100.0 (2026-05-09 v3 audit;
# 0.97 = CMA Memory beta, 0.95 = Bedrock auth breaking change).
anthropic==0.100.0

# Crypto (vault.py AES-256-GCM, sync_tls). 46.0.7 patches CVE-2026-39892;
# 48.0.0 = currently tested in dev (2026-05-09 audit v3).
cryptography==48.0.0

# HTTP client used transitively by anthropic/requests; 2.7.0 stable.
urllib3==2.7.0

# Used by sync_backend git ops + benchmark scripts.
requests==2.33.1

# Used by .claude/settings parsers + a few CI inline scripts.
PyYAML==6.0.3

# Property testing (forge --gen-props consumers). 6.152.2 currently tested.
hypothesis==6.152.2

# CRIT-2 (2026-05-10): freeze datetime in tests that compute time deltas
# (test_tier1_a2.py, test_tier3_c1.py). Without this lock, the test pair
# fails 50% of the time when the run crosses midnight UTC.
freezegun==1.5.5

# Phase 4 (2026-05-09): forge-shield (split out of MUNINN- on 2026-05-07,
# polished cycles 4→8 on sky1241/forge). v1.1.0 adds --modularity
# (Newman Q + Louvain), --fast-deep (transitive impact),
# --paths-to-mutate, --incremental-mutate (libcst AST-diff).
# H1 (2026-05-09): the 3 legacy forge.py copies (root + engine/core +
# muninn) were removed — PyPI forge-shield 1.1.1 is the single source
# of truth. cube_analysis.py and test_props_forge.py now `from forge`.
#
# Note: v1.1.0 is not on PyPI yet (PyPI has 1.0.4). CI installs from
# git tag (.github/workflows/ci.yml). Local dev: pip install from git
# tag too — see README.
# forge-shield  (no constraint — install from git+v1.1.1 in CI)
